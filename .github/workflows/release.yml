name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'
      
      - name: Build Linux AMD64
        run: |
          GOOS=linux GOARCH=amd64 go build -o baracuda-linux-amd64 .
          tar czf baracuda-linux-amd64.tar.gz baracuda-linux-amd64
      
      - name: Build Linux ARM64
        run: |
          GOOS=linux GOARCH=arm64 go build -o baracuda-linux-arm64 .
          tar czf baracuda-linux-arm64.tar.gz baracuda-linux-arm64
      
      - name: Build macOS AMD64
        run: |
          GOOS=darwin GOARCH=amd64 go build -o baracuda-darwin-amd64 .
          tar czf baracuda-darwin-amd64.tar.gz baracuda-darwin-amd64
      
      - name: Build macOS ARM64
        run: |
          GOOS=darwin GOARCH=arm64 go build -o baracuda-darwin-arm64 .
          tar czf baracuda-darwin-arm64.tar.gz baracuda-darwin-arm64
      
      - name: Build Windows AMD64
        run: |
          GOOS=windows GOARCH=amd64 go build -o baracuda-windows-amd64.exe .
          zip baracuda-windows-amd64.zip baracuda-windows-amd64.exe
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            baracuda-linux-amd64.tar.gz
            baracuda-linux-arm64.tar.gz
            baracuda-darwin-amd64.tar.gz
            baracuda-darwin-arm64.tar.gz
            baracuda-windows-amd64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

